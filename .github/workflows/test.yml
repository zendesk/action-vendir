name: Test

on: push

jobs:
  test_run:
    name: test
    runs-on: ubuntu-latest
    steps:
      - uses: zendesk/checkout@v2
      - name: Run vendir
        uses: ./
        with:
          token: ${{ secrets.GLOVER_TEST_KEY }}